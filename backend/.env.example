# 백엔드 로컬 실행용 환경변수 템플릿.
# 이 파일을 복사해 backend/.env 로 만들고 실제 값을 채운다(.env 는 gitignore됨).
# 실행: cd backend && set -a && source .env && set +a && ./gradlew bootRun

# 로컬 MySQL
DB_PASSWORD=

# JWT 시크릿 (HS256, 32바이트 이상)
JWT_SECRET=

# 카카오 OAuth (콘솔 발급값)
KAKAO_CLIENT_ID=
KAKAO_CLIENT_SECRET=
