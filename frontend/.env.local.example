# 카카오 REST API 키 (카카오 개발자 콘솔 > 앱 키 > REST API 키).
# 브라우저의 인가 요청(authorize)에 사용되므로 NEXT_PUBLIC_ 접두어가 필요하다.
# 실제 값은 .env.local 에 넣는다 (git에 커밋하지 않음).
NEXT_PUBLIC_KAKAO_CLIENT_ID=

# 카카오 콘솔의 Redirect URI 에 http://localhost:3000/login/kakao/callback 을 등록해야 한다.
